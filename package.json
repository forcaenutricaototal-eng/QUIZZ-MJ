{
  "name": "quiz-mj-vercel-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Dependencies for Vercel Serverless Functions",
  "scripts": {
    "build": "mkdir -p public/dist && cp index.html public/ && esbuild index.tsx --bundle --outfile=public/dist/index.js --jsx=automatic --allow-overwrite"
  },
  "dependencies": {
    "@google/genai": "^1.24.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "esbuild": "0.23.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vercel/node": "^5.3.26"
  }
}